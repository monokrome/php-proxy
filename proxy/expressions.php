<?php
	/**
	 * URL rules / expressions.
	 * TODO: Make it so that people can provide a custom method name to allow strings other than regex for comparing.
	 */

	// Add regular expressions that match your URLs to this Array. Always use secure expressions, or you will be sad.
	return Array();
?>
